import * as linez from 'linez';
import eclint = require('./eclint');
export declare class Line implements linez.Line {
    isBlockComment?: boolean;
    isBlockCommentEnd: boolean;
    isBlockCommentStart: boolean;
    blockCommentStart?: Line;
    doc: Document;
    padSize: number;
    prefix: string;
    string: string;
    suffix: string;
    offset: number;
    number: number;
    ending: string;
    constructor(line: linez.Line, doc?: Document);
    text: string;
}
export declare function create(file: any, settings?: eclint.Settings): Document;
export interface Document extends linez.Document {
    lines: Line[];
}
